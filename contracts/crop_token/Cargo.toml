[package]
name = "crop_token"
version = "0.1.0"
edition = "2021"
publish = false
description = "Contrato Soroban CosechaCoin — tokenización de cosechas agrícolas"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[package.metadata.stellar]
cargo_inherit_metadata = true
